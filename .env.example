# Postgres connection string.
# Default below matches docker-compose.yml. For Neon / Vercel Postgres, use your connection string.
DATABASE_URL="******localhost:5432/charitybox?schema=public"

# Set to "true" to enable POST /api/dev/donate. Leave unset or "false" in production.
ALLOW_FAKE_DONATIONS="true"
